{
  "name": "gbc-mobile-menu",
  "version": "1.0.0",
  "description": "makes the mobile menu json nice, reduces size and minifies",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js --exec babel-node --presets es2015,stage-2"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-2": "^6.18.0",
    "fetch": "^1.1.0"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
